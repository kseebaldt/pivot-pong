---
buildpack: ruby_buildpack
services:
  - database
env:
  RACK_ENV: production
  RAILS_ENV: production
  pong_location: Seattle
applications:
  - name: seattle-pong
    command: bundle exec rake server
